version: "3"

services:
  rosbag2dataset:
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - ./:/root/rosbag2dataset
      - ~/dataset:/root/bagfiles
      - ~/ptfiles:/root/dataset
    stdin_open: true
    privileged: true